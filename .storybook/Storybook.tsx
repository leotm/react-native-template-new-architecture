/**
 * TODO: Migrate RN Storybook from 6.5.x to 10 (RN 0.71 to 0.86.0-nightly-20260313-972a30db4)
 */

import './doctools'
// @ts-ignore TS2882 noUncheckedSideEffectImports enabled in TS6 by default, storybook.requires.js generated by storybook
import './storybook.requires'

import { getStorybookUI } from '@storybook/react-native'
// import { SafeAreaView } from 'react-native'

// https://github.com/storybookjs/react-native#getstorybookui-options
const StorybookUIRoot = getStorybookUI({
  enableWebsockets: true // for @storybook/react-native-server
  // initialSelection: { kind: 'TextInput', name: 'Basic' },
  // shouldPersistSelection: false,
  // onDeviceUI: false
})

export default () => (
  // <SafeAreaView>
  <StorybookUIRoot />
  // </SafeAreaView>
)
